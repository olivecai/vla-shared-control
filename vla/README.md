# VLA computations 

There should only exist VLA computations and no shared control or human control data outputs sent here. 

inputs: 
- vision (read over ROS node)
- language

outputs:
- vla_action (write over ROS node)